repos:
  - repo: local
    hooks:
      - id: spotless-apply
        name: Spotless Format (Java)
        entry: ./mvnw spotless:apply -q
        language: system
        files: \.(java)$
        pass_filenames: false
        stages: [pre-commit]
        verbose: false

  - repo: local
    hooks:
      - id: spotless-check
        name: Spotless Check (Java)
        entry: ./mvnw spotless:check -q
        language: system
        files: \.(java)$
        pass_filenames: false
        stages: [pre-push]
        verbose: false
