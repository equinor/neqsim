# GitHub integration test

This documentation-only file was added to verify the pull request workflow through the GitHub integration.

It does not change NeqSim code, tests, dependencies, or runtime behavior. The pull request is intended to be closed without merging after the workflow has been verified.
