/*
 * System_SRK_EOS.java
 *
 * Created on 8. april 2000, 23:05
 */

package thermo.system;

import thermo.phase.*;
/**
 *
 * @author  Even Solbraa
 * @version
 */

/** This class defines a thermodynamic system using the SRK equation of state
 */
public class SystemVdWEOS extends SystemEos {
    /** Creates a thermodynamic system using the SRK equation of state. */
    
    //  SystemSrkEos clonedSystem;
    
    public SystemVdWEOS(){
        super();
        attractiveTermNumber = 5;
        for (int i=0;i<numberOfPhases;i++){
            phaseArray[i] = new PhaseVdW();
        }
    }
    
    public SystemVdWEOS(double T, double P) {
        super(T,P);
        attractiveTermNumber = 5;
        for (int i=0;i<numberOfPhases;i++){
            phaseArray[i] = new PhaseVdW();
            phaseArray[i].setTemperature(T);
            phaseArray[i].setPressure(P);
        }
    }
    
    public SystemVdWEOS(double T, double P, boolean solidCheck) {
        this(T,P);
        attractiveTermNumber = 5;
        numberOfPhases = 4;
        maxNumberOfPhases = 4;
        solidPhaseCheck = solidCheck;
        
        for (int i=0;i<numberOfPhases;i++){
            phaseArray[i] = new PhaseVdW();
            phaseArray[i].setTemperature(T);
            phaseArray[i].setPressure(P);
        }
        
        if(solidPhaseCheck){
            //System.out.println("here first");
            phaseArray[numberOfPhases-1] = new PhasePureComponentSolid();
            phaseArray[numberOfPhases-1].setTemperature(T);
            phaseArray[numberOfPhases-1].setPressure(P);
            phaseArray[numberOfPhases-1].setRefPhase(phaseArray[1].getRefPhase());
        }
    }
    
    public Object clone(){
        SystemVdWEOS clonedSystem = null;
        try{
            clonedSystem = (SystemVdWEOS) super.clone();
        }
        catch(Exception e) {
            e.printStackTrace(System.err);
        }
        
//        clonedSystem.phaseArray = (PhaseInterface[]) phaseArray.clone();
//        for(int i = 0; i < numberOfPhases; i++) {
//            clonedSystem.phaseArray[i] = (PhaseInterface) phaseArray[i].clone();
//        }
        
        return clonedSystem;
    }
    
    
}